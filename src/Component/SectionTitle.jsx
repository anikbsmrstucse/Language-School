import React from 'react';

const SectionTitle = ({title}) => {
    return (
        <div>
            <h1 className='text-5xl text-center font-semibold my-20'>{title}</h1>
        </div>
    );
};

export default SectionTitle;